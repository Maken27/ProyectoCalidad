export interface Employee{
    vender?: number; //Si tiene ? -> Significa que es opcional.
    buscar: String;
    eliminar: String;
    phone: String;
    email: String; 
    created_ad?: String;
    update_ad?: String;
}