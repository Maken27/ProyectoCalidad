export interface Employee{
    id?: number; //Si tiene ? -> Significa que es opcional.
    name: String;
    address: String;
    phone: String;
    email: String; 
    created_ad?: String;
    update_ad?: String;
}